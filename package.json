{
  "name": "scan-memecoin-research",
  "version": "0.1.0",
  "description": "SCAN - rapid memecoin research and risk-analysis assistant for Firefox (Phase 1 MVP, mock data layer)",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node tools/validate-manifest.mjs",
    "build": "node tools/build.mjs",
    "verify": "npm run check && npm test && npm run build"
  },
  "engines": { "node": ">=20" }
}
